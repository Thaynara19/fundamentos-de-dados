# 📊 Fundamentos de Dados — Aprendizado do Dia

## ✅ Conteúdo Estudado

Hoje continuei meus estudos em **Fundamentos de Dados**, aprendendo mais sobre:

- Uso de **planilhas**
- Criação e análise de **gráficos**
- Introdução prática às consultas em **SQL**

O estudo foi focado em entender como os dados são organizados antes de serem utilizados em bancos de dados relacionais.

---

## 📈 Planilhas e Gráficos

Aprendi que as planilhas são importantes porque:

- Organizam dados estruturados
- Permitem gerar gráficos automaticamente
- Facilitam análises visuais
- Servem como base inicial antes da utilização em bancos SQL

Os gráficos ajudam a interpretar informações de forma visual, facilitando a tomada de decisão baseada em dados.

---

## 🗄️ Introdução ao SQL

Durante o estudo, compreendi melhor alguns comandos fundamentais do SQL:

### 🔹 SELECT
Responsável por **selecionar dados** de uma tabela.

```sql
SELECT coluna
No meu exemplo inicial ficaria assim :

SELECT irmão FROM * idade WHERE <= '20';, porém me apronfundei mais no estudo e percebi que o FROM indica a tabela e o WHERE filtra registro
específicos.

No caso a formula correta seria : SELECT * FROM irmão WHERE idade <=20;
